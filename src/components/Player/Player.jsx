import PropTypes from 'prop-types'; 

const Player = ({player}) => {
      return (
            <div>
                  
            </div>
      );
};

Player.propTypes = {
      player: PropTypes.object.isRequired
}

export default Player;